## LoRa Boat Monitor Documentation Server

This directory now only keeps the static assets that are still used by the lightweight GitHub Pages container:

- `firmware/ActualVersion.txt`
- `firmware/.gitkeep`
- shared page assets such as `favicon.ico` and `style.css`

Historical HTML documentation snapshots, screenshots, and archived firmware binaries were removed from the repository to keep the update payload and maintenance surface small.
